export declare namespace Filter {
    type Options = {
        label: string;
        input: {
            placeholder: string;
            maxlength?: number;
            pattern?: string;
        };
        predicate: (data: string, input: string) => boolean;
    };
    const OPTIONS: {
        equals: Options;
        notEqual: Options;
        contains: Options;
        notContains: Options;
        startsWith: Options;
        endsWith: Options;
        lessThan: Options;
        lessThanOrEquals: Options;
        greaterThan: Options;
        greaterThanOrEquals: Options;
    };
    const optionsString: Options[];
    const optionsNumber: Options[];
}
